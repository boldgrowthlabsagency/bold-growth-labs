/* Retired. Concept previews now live in components/concepts/ — each build has
   its own identity rather than sharing one recoloured template. This file is
   intentionally left as an inert stub so an older copy on disk cannot break
   the build. Safe to delete. */
export default function SitePreview() { return null; }
